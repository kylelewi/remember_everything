import React from 'react';
import Landing from './landing';
import Session from './session';

const Home = props => (
  <div className="home">
    <Landing />
    <Session />
  </div>
);

export default Home;
